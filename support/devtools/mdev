#!/usr/bin/env python3
"""mdev -- single-entrypoint Movian dev/test harness CLI (issue #85).

Wraps the whole dev iteration ritual: isolated launch, route open,
screenshot, prop inspection, log triage, live UI reload and a watch loop.

Run `mdev --help` for the list of subcommands.
"""

import os
import sys

# Keep the repo clean: never litter support/devtools with __pycache__.
sys.dont_write_bytecode = True

sys.path.insert(0, os.path.dirname(os.path.realpath(__file__)))

from mdevlib.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
