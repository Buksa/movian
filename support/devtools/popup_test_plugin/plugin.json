{
  "type": "ecmascript",
  "id": "devtools_popup_test",
  "title": "popup test plugin (dev only, not packaged)",
  "version": "1.0.0",
  "category": "other",
  "synopsis": "Dev-only fixture for movian#242 and #247. Three routes: one that raises a blocking popup from inside its own handler, one that does not, and one gated behind a persisted setting -- the shape a plugin behind a terms prompt has, and the subject of the seeded/unseeded pair. Lives under support/, never shipped.",
  "file": "popup_test.js",
  "apiversion": 2
}
